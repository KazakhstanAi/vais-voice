# Notebooks

Exploration only. Pin manifest/configuration versions and seeds. Clear private outputs and move reusable processing/metrics into tested package modules. No datasets, training or demo results are included.
